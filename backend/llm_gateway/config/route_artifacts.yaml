route_artifacts:
  - route_artifact_id: route.chat_structured.2026_06_27.001
    artifact_digest: sha256:f7344e06e783e27ecdf0dbd593eea71f47e246325a531079189cdce46e114f0e
    lane_id: omi:auto:chat-structured
    surface: openai.chat_completions
    primary:
      provider: openai
      model: gpt-5.4-nano
    fallbacks:
      - provider: openai
        model: gpt-5.4-nano
    provider_options:
      reasoning_effort: low
    timeouts:
      request_ms: 30000
    retry:
      max_attempts: 1
    capabilities:
      text_input: true
      streaming: false
      structured_output: json_schema
      tools: false
    evidence:
      benchmark_snapshot: bench.omi.chat_structured.2026_06_27
      eval_report: eval.chat_extraction_requires_context.2026_06_27
      benchmark_source: omi_eval
      dev_only: false
    rollout:
      stage: shadow
      percent: 0
    credential_policy:
      fallback_eligible_failure_classes:
        - timeout_before_output
        - provider_429_omi_paid
        - provider_5xx_omi_paid
      never_fallback_failure_classes:
        - capability_mismatch
        - provider_invalid_request
        - invalid_config
    fallback_policy:
      fallback_on:
        - timeout_before_output
        - provider_429_omi_paid
        - provider_5xx_omi_paid
      never_fallback_on:
        - capability_mismatch
        - provider_invalid_request
        - invalid_config

  - route_artifact_id: route.chat_structured.2026_06_20.001
    artifact_digest: sha256:292b075551d77500b4ab577800dffaceea473c24a4a728fc461f792b6e471c84
    lane_id: omi:auto:chat-structured
    surface: openai.chat_completions
    # The gateway LKG mirrors the active model so shadow serving and fallback use
    # the same gateway-only model policy without changing legacy chat extraction.
    primary:
      provider: openai
      model: gpt-5.4-nano
    fallbacks: []
    provider_options:
      reasoning_effort: low
    timeouts:
      request_ms: 30000
    retry:
      max_attempts: 1
    capabilities:
      text_input: true
      streaming: false
      structured_output: json_schema
      tools: false
    evidence:
      benchmark_snapshot: bench.omi.chat_structured.2026_06_20
      eval_report: eval.chat_extraction_requires_context.2026_06_20
      benchmark_source: omi_eval
      dev_only: false
    rollout:
      stage: active
      percent: 100
    credential_policy:
      fallback_eligible_failure_classes:
        - timeout_before_output
        - provider_429_omi_paid
        - provider_5xx_omi_paid
      never_fallback_failure_classes:
        - capability_mismatch
        - provider_invalid_request
        - invalid_config
    fallback_policy:
      fallback_on:
        - timeout_before_output
        - provider_429_omi_paid
        - provider_5xx_omi_paid
      never_fallback_on:
        - capability_mismatch
        - provider_invalid_request
        - invalid_config
