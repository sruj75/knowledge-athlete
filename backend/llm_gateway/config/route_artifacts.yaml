route_artifacts:
  - route_artifact_id: route.chat_structured.2026_06_27.001
    artifact_digest: sha256:667faeaf961a3dbbe357e0671d36ee171dd1c7bdee643d04583a2b46ee687b1b
    lane_id: omi:auto:chat-structured
    surface: openai.chat_completions
    primary:
      provider: openai
      model: gpt-5.4-nano
    fallbacks:
      - provider: openai
        model: gpt-5.4-nano
    provider_options:
      reasoning_effort: low
    timeouts:
      request_ms: 30000
    retry:
      max_attempts: 1
    capabilities:
      text_input: true
      streaming: false
      structured_output: json_schema
      tools: false
    evidence:
      benchmark_snapshot: bench.omi.chat_structured.2026_06_27
      eval_report: eval.chat_extraction_requires_context.2026_06_27
      benchmark_source: omi_eval
      dev_only: false
    rollout:
      stage: shadow
      percent: 0
    fallback_policy:
      fallback_on:
        - timeout_before_output
        - provider_429_omi_paid
        - provider_5xx_omi_paid
      never_fallback_on:
        - capability_mismatch
        - provider_invalid_request
        - invalid_config

  - route_artifact_id: route.chat_structured.2026_06_20.001
    artifact_digest: sha256:93bc996bc789ae0da103462f969708fa034aa3d6d552373a4ff58b1b4872f743
    lane_id: omi:auto:chat-structured
    surface: openai.chat_completions
    # The gateway LKG mirrors the active model so shadow serving and fallback use
    # the same gateway-only model policy without changing legacy chat extraction.
    primary:
      provider: openai
      model: gpt-5.4-nano
    fallbacks: []
    provider_options:
      reasoning_effort: low
    timeouts:
      request_ms: 30000
    retry:
      max_attempts: 1
    capabilities:
      text_input: true
      streaming: false
      structured_output: json_schema
      tools: false
    evidence:
      benchmark_snapshot: bench.omi.chat_structured.2026_06_20
      eval_report: eval.chat_extraction_requires_context.2026_06_20
      benchmark_source: omi_eval
      dev_only: false
    rollout:
      stage: active
      percent: 100
    fallback_policy:
      fallback_on:
        - timeout_before_output
        - provider_429_omi_paid
        - provider_5xx_omi_paid
      never_fallback_on:
        - capability_mismatch
        - provider_invalid_request
        - invalid_config
